# Immortan-Project

수정해야 되는 점
1. 우회전 인식 못함
2. park 인식 못함
3. 좌측 기준으로 감
(alexander123)
  좌측기준으로 감 
    이동기준을 좀더 왼쪽으로 붙어서 가게 수정 방법
      lane_detect.py 파일에서 168행
   angle = math.degrees(-math.atan((center_pos - (w / 2.0)) / (h / 2.0)))
           값을 수정 >>> 오른쪽으로 더 떨어져서 달리고 싶다면 목표 기준점을 w / 2.0보다 왼쪽(예: w / 2.0 - 50)으로 옮겨야 하고, 반대로 왼쪽 선에 더 바짝 붙어서 달리고 싶다면 기준점을 오른쪽(예: w / 2.0 + 50)으로 옮겨야 합니다.  + -
                       확인 필요함
  우회전 각도 완만하게 수정 방법
    lane_detect.py 파일에서 201행
    cv2.line(img, (min_x, y), (640, y), (255, 255, 255), 50)
   값을 수정

수정 코드 (더 완만하게 꺾기): 시작점을 오른쪽으로 50픽셀 밀고, 선 두께를 가늘게(30) 줄입니다.
cv2.line(img, (min_x + 50, y), (640, y), (255, 255, 255), 30)

4. 기본 움직이는 속도 증가시키기 
5. PID 제어 및 속도 향상
  self_driving 36열 self.pid = pid.PID(0.4,0.0,0.05) 
  ->pid.PID(0.8,0.1,0.05)를 추천하고 있으며 이때 linear x 값은 0.1로 설명을 했음
  만약 속도(linear x값)를 늘리고 싶다면 P를 조금씩 늘리고(엑셀을 밟는 효과) I는 값유지 혹은 살짝 낯추기 D는 무조건 높이기(브레이크 효과)
  차가 덜컹 거릴 때 -> p값을 낮추고 d값을 올림
  pid.py의 103행 x_pid.setpoint값 수정하기 -> 속도를 올리는 역할(default 5 -> 10정도로 수정)

6/24
1. yaml 파일에서 RGB색상 확인하기
2. crosswalk 확인 후 멈추는 과정이 시간 단위로 되어 있음
3. lane detect 후에 우회전은 가능한데 표지판을 확인 후 우회전 가능하게
4. 병목 현상 해결(로그 주석 처리 해보기, 네트워크 세팅)
5. git setting 하기

6/25
1. 횡단보도 인식
2. 빨간 불에서 횡단보도 앞에서 정지 & 정차한 위치에서 초록 불일 때 기동
3. 레인 트레킹 코너링 값
4. 파킹 구현
5. 시작할 때 초기값 세팅 방법(시작 프로세스 go로 설정)
6. ros 구동 시 새로운 종료 방법 모색(컨트롤 c이외의 방법)


6/26
개선사항
1. 신호등과 횡단보도 분리
2. log가 안 나오고 멈춰있는 경우 -> build, log, install을 지우고 colcon build를 진행하면 되돌릴 수 있음
3. ekf node8 시간지연 오류 관련 -> src/driver/controller/config 내의 ekf.yaml의 8행에 frequecny가 늦기 때문에 발생하는 오류이므로 frequency를 낮춰주면 가능함(현재 값: 100 -> 50)

6/27~28
개선사항
1. 신호등과 횡단보도 분리
2. log가 안 나오고 멈춰있는 경우 -> build, log, install을 지우고 colcon build를 진행하면 되돌릴 수 있음

6/29

TODO
1. 우회전 & 라인 탐지 보이기만 하면 잘 찾지만 간헐적으로 못 찾는 현상이 있음
2. 하드 코딩 되어 있는 부분 손 보기(count & distance)
3. parking 구현하기
4. 무한루프 break부분 구현(차선을 이탈했을 때 버벅이는 현상)
5. 횡단보도 인식 부분 개선 -> yolo annotation을 진행해야 할 거 같음


6/29 breifing & TODO
1. lane detecting 후 좌편향 되어 주행하는 것
2. 신호등 부분 구현
3. yolo 모델 재학습(green, red)-> green을 포착하지 못하는 상황이라면 red로만 제어해보기
4. 우회전, 주차 하드코딩
5. 금요일 발표문서 정리(hease)
6. 추가적인 git setting

6/30 breifing & TODO
1. 현재 parking(jay) & 우회전 부분(hease) & crosswalk 사이즈 제한 설정(sysy009) 제일 좋은 상황
-> 우회전 표지판 포착 시 회전하도록 하드코딩
2. 신호등 detecting 개선(신호등의 green 크기에 대한 제한을 설정하는 것으로 detect 부분 개선 가능)
3. lane detect 후 전체적으로 차체 죄편향되어 있는 부분 수정
4. 속도(linear_x, 0.3) & 회전 각(angular_z,-0.8) default 값 수정
-> lane detect 후의 시간이 달라질 수 있음
5. led 장착 및 구현
6. 기판의 발열로 인한 버벅임
7. 처음 시작 시 lane detect하지 못하면 출발하지 않는 경우가 있음
8. 첫 번째 코너링 전에 갑자기 파킹하는 부분 -> 파킹 표지판 사이즈 크기의 조건을 추가해주면 어느 정도 조절 가능

